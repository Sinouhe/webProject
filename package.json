{
  "name": "rag-cv-monorepo",
  "private": true,
  "packageManager": "pnpm@10.22.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "pnpm -C apps/api start:dev & pnpm -C apps/web dev",
    "dev:api": "pnpm -C apps/api start:dev",
    "dev:web": "pnpm -C apps/web dev",
    "build": "pnpm -C packages/rag build && pnpm -C apps/api build && pnpm -C apps/web build",
    "typecheck": "pnpm -C packages/rag typecheck && pnpm -C apps/api typecheck && pnpm -C apps/web typecheck",
    "lint": "pnpm -C apps/api lint && pnpm -C apps/web lint",
    "rag:build": "pnpm -C packages/rag build",
    "rag:typecheck": "pnpm -C packages/rag typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.10.4",
    "ts-node": "^10.9.2",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
